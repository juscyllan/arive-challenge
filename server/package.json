{
  "name": "@arive/server",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "tsnd --respawn --transpile-only --ignore-watch node_modules --no-notify src/main.ts",
    "build": "tsc -b",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "dependencies": {
    "fastify": "^3.19.1"
  }
}